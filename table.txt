DROP TABLE IF EXISTS People;

CREATE TABLE IF NOT EXISTS People (
    id int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY ,
    name VARCHAR(30) NOT NULL,
    current_cell INTEGER NOT NULL ,
    first_dice INTEGER NOT NULL ,
    second_dice INTEGER NOT NULL ,
    result INTEGER NOT NULL,
    game_status VARCHAR(30) NOT NULL
);

INSERT INTO People (name, current_cell, first_dice, second_dice, result, game_status) VALUES ('Ivan', 0, 0, 0, 0, 'Roll the dice');
INSERT INTO People (name, current_cell, first_dice, second_dice, result, game_status) VALUES ('Andrey', 0, 0, 0, 0, 'Roll the dice');
INSERT INTO People (name, current_cell, first_dice, second_dice, result, game_status) VALUES ('Peter', 0, 0, 0, 0, 'Roll the dice');

SELECT * FROM People;